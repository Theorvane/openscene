# Open-Source Baseline

Issue #3 establishes public contributor documents and deterministic macOS FFmpeg CI. Keep contribution work issue-driven: branch from `dev`, target pull requests to `dev`, and use separate reviewed `dev` to `main` release pull requests. Never place sensitive security material in public templates or issues.
